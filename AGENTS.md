# AGENTS.md

## 项目名称

产品展示与智能报价系统小程序

## 项目目标

开发一个微信小程序 + PC 管理后台 + 后端服务系统，用于企业产品展示、解决方案展示、案例展示、客户询价、销售报价、Word 报价书生成、标准配置模板、产品关联推荐、AI 知识库问答。

## 系统组成

- 微信小程序端
- PC 管理后台
- Spring Boot 后端服务
- AI 知识库服务，第一阶段只预留接口

## 技术栈

- 后端：Java 17、Spring Boot 3.x、MyBatis Plus、MySQL 8、Redis、MinIO、Sa-Token 或 Spring Security
- PC 后台：Vue3、TypeScript、Element Plus、Pinia、Axios
- 微信小程序：微信原生小程序、Vant Weapp
- AI 服务：第一阶段只预留接口，暂不实现；后续规划 Python FastAPI、LangChain、Qdrant、DeepSeek / 通义千问 / OpenAI

## 第一阶段开发目标

先完成基础框架和核心业务 CRUD。AI 知识库问答、Word 报价书真实生成、微信订阅消息真实发送、电子签章、CRM、合同管理在第一阶段只保留数据结构或接口占位。

## 业务规则

- 产品一级分类固定为：传感器、采集仪、通信、安装附件、供电系统、安装耗材。
- 报价支持多个测项；每个测项依次确认六类产品，每一步都必须选择或跳过。
- 产品可配置关联产品，报价时选择产品后应返回关联推荐产品列表。
- AI 助手当前仅销售人员和技术工程师可使用；第一阶段只预留文档上传记录表、AI 问答接口占位、问答记录表，暂不实现真实向量检索。

## UI 风格

- 科技感、专业感、企业级、工程化
- 主色：蓝色 `#1677FF`
- 避免电商风、花哨动画、过度营销化界面

## 开发要求

- 代码结构清晰，后端按模块分包。
- 所有表包含 `create_time`、`update_time`、`deleted` 字段。
- 删除使用逻辑删除。
- 列表接口支持分页。
- 管理后台所有列表支持搜索。
- 所有金额字段使用 `BigDecimal`。
- 文件统一走 MinIO 抽象服务。
- 所有接口返回统一 Response 结构。
- 先保证可运行，再逐步完善业务细节。
